{
  "name": "@bravevest/db",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    },
    "./prisma": {
      "import": "./prisma/index.js"
    }
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "clean": "rm -rf dist",
    "db:generate": "dotenv -e ../../apps/api/.env -- prisma generate",
    "db:push": "dotenv -e ../../apps/api/.env -- prisma db push",
    "db:studio": "dotenv -e ../../apps/api/.env -- prisma db studio",
    "db:migrate": "dotenv -e ../../apps/api/.env -- prisma migrate dev",
    "db:deploy": "dotenv -e ../../apps/api/.env -- prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "vite": "^5.0.0"
  }
}